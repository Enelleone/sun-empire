// هذا الملف يمكن استخدامه لإضافة وظائف JavaScript لاحقًا.
// حاليًا هو فارغ، لكنه ضروري لربطه بملف HTML.

// مثال بسيط: رسالة ترحيب في الكونسول للمطورين
console.log("مرحبًا بك في موقع Connectify!");

// مثال على كود لزر الوضع المظلم (يتطلب تعديل في HTML و CSS):
/*
document.addEventListener('DOMContentLoaded', () => {
    const darkModeToggle = document.getElementById('dark-mode-toggle'); // افترض أن لديك زر بهذا الـ ID في HTML
    if (darkModeToggle) {
        darkModeToggle.addEventListener('click', () => {
            document.body.classList.toggle('dark-mode');
            // يمكنك حفظ تفضيل المستخدم في Local Storage
            if (document.body.classList.contains('dark-mode')) {
                localStorage.setItem('theme', 'dark');
            } else {
                localStorage.setItem('theme', 'light');
            }
        });
    }

    // تحميل التفضيل عند فتح الصفحة
    if (localStorage.getItem('theme') === 'dark') {
        document.body.classList.add('dark-mode');
    }
});
*/
